{
  "name": "stm-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.25.0",
  "scripts": {
    "dev": "pnpm --filter app dev",
    "build": "pnpm -r --filter \"@thinking-home/*\" build",
    "build:app": "pnpm --filter app build",
    "test": "vitest run",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
